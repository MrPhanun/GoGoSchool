PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

DB_SERVER=localhost
DB_PORT=1433
DB_NAME=GoGoSchoolDB
DB_USER=gogo_app
DB_PASSWORD=change-this-password

# Set both to true for Azure SQL / most cloud-hosted SQL Server (requires
# encrypted connections with a trusted cert). Leave unset for local dev.
# DB_ENCRYPT=true
# DB_TRUST_CERT=false
